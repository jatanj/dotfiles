#!/bin/bash
program="$1"
if [[ $(pidof "$program" 2> /dev/null) ]]; then
	jumpapp -t "$program" xfce4-terminal
elif [[ ! -e $(which "$program" 2> /dev/null) ]]; then
	xfce4-terminal "$@"
else
	shift
	xfce4-terminal --title="$program" -e "$program" "$@"
fi
